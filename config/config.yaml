# config/default.yaml
# Configuration file used by src.main if additional hyper-parameters are
# required in future iterations.  Currently all options are provided through
# CLI so this YAML is intentionally minimal.
train_steps: 300
lr: 1e-4
